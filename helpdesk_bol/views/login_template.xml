<odoo>

    <template inherit_id="web.login" id="login" name="Login">
        <xpath expr="//div[hasclass('field-login')]" position="replace">
            <div class="mb-3 field-login">
                <label for="login" class="form-label">User</label>
                <input type="text" placeholder="User" name="login" t-att-value="login" id="login" t-attf-class="form-control #{'form-control-sm' if form_small else ''}" required="required" autocomplete="username" autofocus="autofocus" autocapitalize="off"/>
            </div>
        </xpath>
    </template>

    <template id="helpdesk_bol.login_socios" name="Login">
        <t t-call="web.login_layout">
            <div class="background" t-attf-style="background-image: url('#{bg_img}'); background-color: #{color}; min-height: 100vh; background-size: cover;">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <input type="hidden" name="redirect" t-att-value="redirect"/>
                <div class="container py-5">
                    <div t-attf-class="card border-0 mx-auto bg-100 {{login_card_classes}} o_database_list" style="max-width: 300px;">
                        <div class="card-body">
                            <div t-attf-class="text-center pb-3 border-bottom {{'mb-3' if form_small else 'mb-4'}}">
                                <img t-attf-src="/web/binary/company_logo{{ '?dbname='+db if db else '' }}"
                                     alt="Logo"
                                     style="max-height:120px; max-width: 100%; width:auto"/>
                            </div>
                            <form class="oe_login_form" role="form" t-attf-action="/web/socios/login" method="post" onsubmit="this.action = '/web/socios/login' + location.hash">
                                <input type="hidden" name="redirect" t-att-value="redirect"/>
                                <div class="mb-3 field-login">
                                    <label for="login" class="form-label">Enter your ID card number</label>
                                    <input type="text" placeholder="ID card number" name="login" t-att-value="login" id="login" t-attf-class="form-control #{'form-control-sm' if form_small else ''}" required="required" autocomplete="username" autofocus="autofocus" autocapitalize="off"/>
                                </div>
                                <p class="alert alert-danger" t-if="error" role="alert">
                                    <t t-esc="error"/>
                                </p>
                                <p class="alert alert-success" t-if="message" role="status">
                                    <t t-esc="message"/>
                                </p>

                                <div t-attf-class="clearfix oe_login_buttons text-center gap-1 d-grid mb-1 {{'pt-2' if form_small else 'pt-3'}}">
                                    <button type="submit" class="btn btn-primary">Log in</button>
                                    <div class="o_login_auth"/>
                                    <input type="hidden" name="redirect" t-att-value="redirect"/>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="portal_my_details_fields" inherit_id="portal.portal_my_details_fields" name="Readonly Portal Fields">
        <xpath expr="//*" position="replace">
            <t/>
        </xpath>
        <xpath expr="." position="inside">
            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
            <div t-if="error_message" class="alert alert-danger" role="alert">
                <div class="col-lg-12">
                    <t t-foreach="error_message" t-as="err"><t t-esc="err"/><br /></t>
                </div>
            </div>
            <div t-if="not partner_can_edit_vat" class="col-12 d-none d-xl-block">
                <small class="form-text text-muted">
                    Company name, VAT Number and country can not be changed once document(s) have been issued for your account.
                    <br/>Please contact us directly for that operation.
                </small>
            </div>
            <div t-attf-class="mb-3 #{error.get('name') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label" for="name">Name</label>
                <input type="text" name="name" t-attf-class="form-control #{error.get('name') and 'is-invalid' or ''}" t-att-value="name or partner.name" readonly="1"/>
            </div>
            <div t-attf-class="mb-3 #{error.get('email') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label" for="email">Email</label>
                <input type="email" name="email" t-attf-class="form-control #{error.get('email') and 'is-invalid' or ''}" t-att-value="email or partner.email" readonly="1"/>
            </div>

            <div class="clearfix" />
            <div t-attf-class="mb-1 #{error.get('vat') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label label-optional" for="vat">VAT Number</label>
                <!-- The <input> use "disabled" attribute to avoid sending an unauthorized value on form submit.
                     The user might not have rights to change company_name but should still be able to see it.
                -->
                <input type="text" name="vat" t-attf-class="form-control #{error.get('vat') and 'is-invalid' or ''}" t-att-value="vat or partner.vat" t-att-disabled="None if partner_can_edit_vat else '1'" readonly="1"/>
                <small t-if="not partner_can_edit_vat" class="form-text text-muted d-block d-xl-none">Changing VAT number is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.</small>
            </div>
            <div t-attf-class="mb-3 #{error.get('phone') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label" for="phone">Phone</label>
                <input type="tel" name="phone" t-attf-class="form-control #{error.get('phone') and 'is-invalid' or ''}" t-att-value="phone or partner.phone" readonly="readonly"/>
            </div>

            <div class="clearfix" />
            <div t-attf-class="mb-3 #{error.get('street') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label" for="street">Street</label>
                <input type="text" name="street" t-attf-class="form-control #{error.get('street') and 'is-invalid' or ''}" t-att-value="street or partner.street" readonly="readonly"/>
            </div>
            <div t-attf-class="mb-3 #{error.get('city') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label" for="city">City</label>
                <input type="text" name="city" t-attf-class="form-control #{error.get('city') and 'is-invalid' or ''}" t-att-value="city or partner.city" readonly="1"/>
            </div>
            <div t-attf-class="mb-3 #{error.get('zip') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label label-optional" for="zipcode">Zip / Postal Code</label>
                <input type="text" name="zipcode" t-attf-class="form-control #{error.get('zip') and 'is-invalid' or ''}" t-att-value="zipcode or partner.zip" readonly="1"/>
            </div>
<!--            <div t-attf-class="mb-3 #{error.get('country_id') and 'o_has_error' or ''} col-xl-6">-->
<!--                <label class="col-form-label" for="country_id">Country</label>-->
<!--                <select name="country_id" t-attf-class="form-select #{error.get('country_id') and 'is-invalid' or ''}" readonly="readonly">-->
<!--                    <option value="" readonly="readonly">Country...</option>-->
<!--                    <t t-foreach="countries or []" t-as="country" readonly="readonly">-->
<!--                        <option t-att-value="country.id" t-att-selected="country.id == int(country_id) if country_id else country.id == partner.country_id.id"  readonly="readonly">-->
<!--                            <t t-esc="country.name" />-->
<!--                        </option>-->
<!--                    </t>-->
<!--                </select>-->
<!--            </div>-->
            <div t-attf-class="mb-3 #{error.get('state_id') and 'o_has_error' or ''} col-xl-6">
                <label class="col-form-label label-optional" for="state_id">State / Province</label>
                <select name="state_id" t-attf-class="form-select #{error.get('state_id') and 'is-invalid' or ''}" >
                    <option value="">select...</option>
                    <t t-foreach="states or []" t-as="state">
                        <option t-att-value="state.id" style="display:none;" t-att-data-country_id="state.country_id.id" t-att-selected="state.id == int(state_id) if state_id else state.id == partner.state_id.id" readonly="readonly">
                            <t t-esc="state.name" />
                        </option>
                    </t>
                </select>
            </div>
        </xpath>
    </template>

<!--    <template id="web.login_successful" name="Login successful">-->
<!--        <t t-call="web.login_layout">-->
<!--            <div class="oe_login_form">-->
<!--                <p>-->
<!--                    You are logged in.-->
<!--                </p>-->
<!--                <div t-attf-class="clearfix oe_login_buttons text-center mb-1 pt-3">-->
<!--                    <a class="btn btn-primary btn-block" href="/web/session/logout">Log out</a>-->
<!--                </div>-->
<!--            </div>-->
<!--        </t>-->
<!--    </template>-->

</odoo>
