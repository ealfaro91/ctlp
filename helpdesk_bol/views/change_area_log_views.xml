<odoo>

    <!-- FORM VIEW -->

    <record id="change_area_log_form_view" model="ir.ui.view">
        <field name="name">change.area.log.form.view</field>
        <field name="model">change.area.log</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="ticket_id" invisible="1"/>
                            <field name="user_id" readonly="1" force_save="1"/>
                        </group>
                         <group>
                            <field name="area_id" readonly="1" force_save="1" widget="badge"/>
                            <field name="date" readonly="1" force_save="1"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread" options="{'post_refresh': 'recipients', 'open_attachments': True}"/>
                </div>
            </form>
        </field>
    </record>

    <!-- TREE VIEW -->

    <record id="change_area_log_tree_view" model="ir.ui.view">
        <field name="name">change.area.log.tree.view</field>
        <field name="model">change.area.log</field>
        <field name="arch" type="xml">
            <tree>
                <field name="ticket_id" column_invisible="1"/>
                <field name="user_id"/>
                <field name="area_id" widget="badge"/>
                <field name="date"/>
            </tree>
        </field>
    </record>


</odoo>
