<?xml version="1.0" encoding="UTF-8"?>
<odoo>
      <data>

         <!-- ===================
              SALE ORDER VIEWS
              =================== -->

          <record model="ir.ui.view" id="sale_order_certificate_button_form_view">
              <field name="name">sale.order.certificate.button.form.view</field>
              <field name="model">sale.order</field>
              <field name="inherit_id" ref="sale.view_order_form"/>
              <field name="arch" type="xml">
                  <button name="action_draft" position="after">
                      <button name="button_print_certificate"
                            string="Imprimir certificado de búsqueda"
                            type="object"
                            invisible="state != 'sale'"/>
                  </button>
              </field>
            </record>

      </data>
</odoo>
